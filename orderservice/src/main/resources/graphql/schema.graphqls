type Query {
    listOrders: [Order]
}
type Mutation {
    createOrder(userId: ID!, productId: ID!, quantity: Int!): Order
}


type Order {
    id: ID!
    userId: ID!
    productId: ID!
    quantity: Int!
}
